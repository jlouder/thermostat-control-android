<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent" >

    <NumberPicker
        android:id="@+id/temp_picker"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="64dp" />

    <Button
        android:id="@+id/temp_picker_cancel_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/temp_picker"
        android:layout_marginLeft="20dp"
        android:layout_marginTop="98dp"
        android:layout_toRightOf="@+id/temp_picker"
        android:text="@string/cancel" />

    <Button
        android:id="@+id/temp_picker_set_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/temp_picker_cancel_button"
        android:layout_alignBottom="@+id/temp_picker_cancel_button"
        android:layout_marginRight="16dp"
        android:layout_toLeftOf="@+id/temp_picker"
        android:text="@string/set" />

</RelativeLayout>